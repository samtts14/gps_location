name: appbar_example
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  line_awesome_flutter: ^1.0.1
  shared_preferences: ^0.5.12+4

  cupertino_icons: ^1.0.0
  sms: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons:

flutter_icons:
  android: true
  ios: true
  image_path: 'assets/icons/LogoPrincipal.png'
  adaptatative_icon_background: 'assets/icons/LogoPrincipal.png'

flutter:
  uses-material-design: true

  assets:
    - assets/
